import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  textArea: {
    width: 200,
    textAlignVertical: "top",
    borderWidth: 1,
  },
  textAreaContainer: {
    marginTop: 100,
  },
})
